namespace = tr_artifact_spawn

tr_artifact_spawn.01 = {
    scope = none
    hidden = yes

    immediate = { # Weird fucking system, but i guess we gotta give specific characters specific item somehow...
        if = {
            limit = {
                exists = character:tr_ds_siegward
                character:tr_ds_siegward = { is_alive = yes }
            }
            create_artifact_cloranthy_ring = { OWNER = character:tr_ds_siegward }
        }
    }
}